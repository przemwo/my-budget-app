import React from 'react';

const Login = () => {
  return(
    <div>
      <h2>Login page</h2>
      <a href="/login">Login</a>
      {' '}
      <a href="/logout">Logout</a>
    </div>
  );
};

export default Login;
